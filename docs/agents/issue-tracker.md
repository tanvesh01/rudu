# Issue Tracker

Issues for this repo live in **GitHub Issues** at `https://github.com/tanvesh01/rudu/issues`.

The `gh` CLI is used for all issue operations (`gh issue list`, `gh issue create`, `gh issue edit`, etc.).

When creating issues from vertical slices, publish them to this tracker with the correct triage label.
